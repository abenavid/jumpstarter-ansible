---
# Writable per-host twin/state JSON used by demo_select_simulator and simulator_git_persist.
simulator_runtime_base_dir: /tmp/jumpstarter/simulated_devices
# Optional YAML/JSON files: <inventory_hostname>.{yml,yaml,json} under this directory.
simulator_definitions_dir: "{{ playbook_dir }}/../simulators"
jumpstarter_exporter_name: "{{ inventory_hostname }}"
