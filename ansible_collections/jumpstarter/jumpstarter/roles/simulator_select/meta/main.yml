---
galaxy_info:
  role_name: simulator_select
  namespace: jumpstarter
  author: Alex Benavides <abenavid@redhat.com>
  description: >
    Resolve per-host file-backed simulator paths under simulator_runtime_base_dir, optionally load
    <inventory_hostname>.* from simulator_definitions_dir, and seed or load twin.json / state.json.
  license: Apache-2.0
  min_ansible_version: "2.16"
  platforms:
    - name: EL
      versions:
        - "9"
dependencies: []
