# Resolve jumpstarter.jumpstarter (and roles) from this repo’s ansible_collections/
# tree when running playbooks from AAP/AWX (/runner/project) or locally without
# ANSIBLE_COLLECTIONS_PATHS.
[defaults]
collections_path = .:~/.ansible/collections:/usr/share/ansible/collections
